<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
  ios:Page.UseSafeArea="true"
  x:Class="CoursesApp.Pages.CoursePage"
  x:DataType="viewModels:CourseViewModel"
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
  xmlns:viewModels="clr-namespace:CoursesApp.ViewModels;assembly=CoursesApp.ViewModels"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
  <ContentPage.Content>
    <StackLayout Margin="20">
      <StackLayout Orientation="Horizontal">
        <Label
          FontSize="30"
          HorizontalTextAlignment="Center"
          Margin="0,0,0,20"
          Text="Course:" />
        <Label
          FontSize="30"
          HorizontalTextAlignment="Center"
          Margin="0,0,0,20"
          Text="{Binding Title}" />
      </StackLayout>
      <Frame
        BackgroundColor="Tomato"
        HasShadow="False"
        Padding="10">
        <StackLayout>
          <StackLayout Orientation="Horizontal">
            <Label Text="Student Count:" />
            <Label Text="{Binding StudentCount}" />
          </StackLayout>
          <StackLayout Orientation="Horizontal">
            <Label Text="Average Student Age:" />
            <Label Text="{Binding AverageStudentValue}" />
          </StackLayout>

          <StackLayout Orientation="Horizontal">
            <Label Text="Most Popular Major:" />
            <Label Text="{Binding HighestMajor}" />
          </StackLayout>
        </StackLayout>
      </Frame>
      <BoxView
        BackgroundColor="DarkGray"
        HeightRequest="2"
        Margin="0,20" />
      <StackLayout BindableLayout.ItemsSource="{Binding Students}" Orientation="Horizontal">
        <BindableLayout.ItemTemplate>
          <DataTemplate x:DataType="viewModels:StudentViewModel">
            <Frame
              HasShadow="False"
              Margin="0"
              Padding="0">
              <StackLayout Margin="0,0,0,10">
                <Frame
                  BackgroundColor="Tomato"
                  CornerRadius="4"
                  HasShadow="False">
                  <StackLayout>
                    <StackLayout Orientation="Horizontal">
                      <Label Text="{Binding Name}" TextColor="Black" />
                      <Label Text="(" TextColor="Black" />
                      <Label Text="{Binding Age}" TextColor="Black" />
                      <Label Text=")" TextColor="Black" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                      <Label Text="{Binding Major}" TextColor="Black" />
                    </StackLayout>
                  </StackLayout>
                </Frame>
              </StackLayout>
            </Frame>
          </DataTemplate>
        </BindableLayout.ItemTemplate>
      </StackLayout>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>