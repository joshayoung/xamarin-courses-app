<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
  x:Class="CoursesApp.Pages.TeacherPage"
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
  <ContentPage.Content>
    <StackLayout Padding="10">
      <Label
        FontAttributes="Bold"
        FontSize="24"
        HorizontalOptions="CenterAndExpand"
        Margin="0,0,0,20"
        Text="Teacher Details" />
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Label
          Grid.Column="0"
          Grid.Row="0"
          Text="Name" />
        <Entry
          Grid.Column="0"
          Grid.Row="1"
          Text="{Binding Name, Mode=TwoWay}" />
        <Label
          Grid.Column="1"
          Grid.Row="0"
          Text="Age" />
        <Entry
          Grid.Column="1"
          Grid.Row="1"
          Text="{Binding Age, Mode=TwoWay}" />
      </Grid>
      <Button
        BackgroundColor="Tomato"
        Text="Update Teacher"
        TextColor="White" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>