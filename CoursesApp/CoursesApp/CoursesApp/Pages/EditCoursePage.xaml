<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
  x:Class="CoursesApp.Pages.EditCoursePage"
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:viewModels="clr-namespace:CoursesApp.ViewModels;assembly=CoursesApp.ViewModels"
  x:DataType="viewModels:CourseViewModel"
  >
  <ContentPage.Content>
    <StackLayout Padding="20">
      <Button
        Clicked="CloseModal"
        FontAttributes="Bold"
        FontSize="40"
        HorizontalOptions="Start"
        Text="𝘅"
        TextColor="Tomato"
        />
      <Label
        Margin="0,0,0,30"
        FontAttributes="Bold"
        FontSize="30"
        HorizontalOptions="CenterAndExpand"
        Text="Edit This Course"
        />
      <Label Text="Course Name:" />
      <Entry Text="{Binding Title, Mode=TwoWay}" />

      <Label Text="Course Length:" />
      <Picker
        ItemDisplayBinding="{Binding .}"
        ItemsSource="{Binding CourseLengthList}"
        SelectedItem="{Binding SelectedLength}"
        />

      <Label Text="Course Type:" />
      <Picker
        ItemDisplayBinding="{Binding .}"
        ItemsSource="{Binding CourseTypesList}"
        SelectedItem="{Binding SelectedType}"
        />
      <Button
        BackgroundColor="Tomato"
        Clicked="SaveCourseEdits"
        Text="Save Edits"
        TextColor="Black"
        />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>